<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:XamFirChatApp.ViewModel"
             x:Class="XamFirChatApp.View.ChatRoomPage"
             Title="Chat Room" 
             >
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:ChatRoomViewModel x:Key="chatRoomVM" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save"
                     Command="{Binding Source={StaticResource chatRoomVM}, Path=SaveSubscriptionCommand}"
                     CommandParameter="{Binding Source={StaticResource chatRoomVM}, Path=Name }"
                     />
    </ContentPage.ToolbarItems>
    <ContentPage.Content> 
        <StackLayout BindingContext="{StaticResource chatRoomVM}" Margin="16">
            <Entry Placeholder="Subscription Name" Text="{Binding Name, Mode=TwoWay}" />
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Label Text="Is this subscription still active?" VerticalOptions="Center" />
                <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay}" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>